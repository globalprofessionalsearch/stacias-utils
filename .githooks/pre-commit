#!/usr/bin/env bash
# Validate + sync skills, regenerate README, and stage it. Fails on bad frontmatter.
set -e
root="$(git rev-parse --show-toplevel)"
"$root/sync-skills"
"$root/gen-readme"
git add "$root/README.md"
